# jWeb frontend

Angular 19 OTP login against [junctionBack](https://github.com/ancqit/junctionBack) (`https://junctionback.onrender.com`).

- `/login` — name + phone → OTP
- `/home` — signed-in workspace

```bash
npm install
npm start
```
